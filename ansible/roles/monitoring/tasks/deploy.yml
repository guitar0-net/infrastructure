# SPDX-FileCopyrightText: 2026 Andrey Kotlyar <guitar0.app@gmail.com>
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Stat docker.sock to get actual group GID
  ansible.builtin.stat:
    path: /var/run/docker.sock
  register: docker_sock_stat

- name: Set docker_gid from host docker.sock
  ansible.builtin.set_fact:
    docker_gid: "{{ docker_sock_stat.stat.gid }}"
  when: docker_sock_stat.stat.exists

- name: Render docker-compose for monitoring stack
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: "{{ monitoring_dir }}/docker-compose.yml"
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0644"

- name: Fix prometheus_data volume ownership for nobody user (UID 65534)
  community.docker.docker_container:
    name: prometheus-chown
    image: alpine
    command: chown -R 65534:65534 /prometheus
    volumes:
      - guitar0-monitoring_prometheus_data:/prometheus
    auto_remove: true
    detach: false

- name: Deploy monitoring stack
  community.docker.docker_compose_v2:
    project_src: "{{ monitoring_dir }}"
    state: present
    pull: always
